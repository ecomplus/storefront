<%
const blogPage = _.cms('blog')
const { slug, content } = await _.resolveRoute()
_.breadcrumbs = [{
  name: blogPage.title,
  link: `/blog`
}, {
  name: content.title,
  link: `/posts/${slug}`
}]
%>

<%- await include('/@/view') %>
