@import "variables";

<% if (bootswatch && bootswatch.length > 1) { %>
  @import "<%- themesPath %>/<%- bootswatch %>/variables";
  @import "bootswatch/dist/<%- bootswatch %>/variables";
<% } %>

@import "@ecomplus/storefront-twbs/scss/styles";

<% if (bootswatch && bootswatch.length > 1) { %>
  @import "bootswatch/dist/<%- bootswatch %>/bootswatch";
  @import "<%- themesPath %>/<%- bootswatch %>/custom";
<% } %>

@import "main";
