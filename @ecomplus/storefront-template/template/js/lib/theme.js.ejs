<% 
  const requireThemeFiles = () => {
    for (const themeColl in theme) {
      const themeName = theme[themeColl]
      if (themeName && themeName.length > 1) {
        return JSON.stringify(themeConfig)
      } else {
        return '{}'
      }
    }
  }
%>

window._theme = <%- requireThemeFiles() %>
